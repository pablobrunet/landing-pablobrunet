// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://pablobrunet.com',
  // La página es 100% estática (SSG). No requiere adaptador ni backend.
});
